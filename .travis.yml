# 
# Configuration for http://travis-ci.org/#!/ari/jobsworth
#
rvm: 1.9.2
branches:
  only:
    - master
    - dev
script: "bundle exec rake db:create db:schema:load db:test:prepare test rspec spec --trace"
before_script:
  - "mysql -e 'create database jobsworth_test;' >/dev/null"