<div id="content">
  <% form_tag(:action => "update") do %>
    <fieldset class="basic">
      <legend><%= _("Custom Attributes") %></legend>
      
      <ul id="attributes">
	<%= render(:partial => "attribute", :collection => @attributes) %>
      </ul>
      
      <br clear="both" />
      <%= link_to_add_attribute %>
      
      <%= submit_tag(_("Update"), :class => "nolabel") %>
    </fieldset>
  <% end %>
</div>
