<tr>
  <td><%= h(property.name) %></td>
  <td><%= link_to(_("Edit"), edit_property_path(property)) %></td>
  <td><%= link_to(_("Delete"), property_path(property), :method => :delete, :confirm => "Are you sure?") %></td>
</tr>
