<% if @old_name.nil? -%>
<%= @user.name %> <%= @action %> the '<%= @milestone.name %>' milestone for the '<%=@milestone.project.customer.name %> / <%=@milestone.project.name %>' project:
<% else -%>
<%= @user.name %> <%= @action %> the '<%= @old_name %>' milestone to '<%= @milestone.name %>' in '<%=@milestone.project.name %>':
<% end -%>
<%= "Due: #{@due_date.strftime('%A, %d %B %Y')}" unless @due_date.nil? %>
<%= "Description:\n#{@milestone.description}" unless @milestone.description.empty? %>

<% unless @action == 'deleted' -%>
<%= @user.company.site_URL%>/views/select_milestone/<%= @milestone.id %>
<% end -%>

-------------------->
<%= $CONFIG[:productName] %>