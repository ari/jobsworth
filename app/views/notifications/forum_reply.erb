<%= @user.name %> replied to a thread you are monitoring or have posted in:

<%= @post.body %>

<% unless @post.forum.company_id.nil? -%>
<%= @user.company.site_URL%>/forums/<%= @post.forum.id %>/topics/<%= @post.topic.id %>#posts-<%= @post.id %>
<% end -%>

-------------------->
<%= $CONFIG[:productName] %>