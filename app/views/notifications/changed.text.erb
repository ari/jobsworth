<%= render(:partial => "task_header") %>

<%= "#{t("notifications.wrote", name: @comment_from)}\n#{@comment}\n".html_safe if @comment.present? -%>

<%= "#{t("notifications.description")}\n#{@task.description}\n".html_safe if @task.description.present? -%>

<%= render(:partial => "task_footer") %>