<%= @task.issue_name%>
<%= "-" * @task.issue_name.length %>

Reverted: <%= @task.status_type %> (by <%= @user.name %>)

<%= "#{@user.name}:\n#{@note}\n" if @note.length > 0 -%>
<%= "\n" if @note.length > 0 -%>
<%= "Description:\n" + @task.description if @task.description.length > 0 %>
--
Message automatically generated by ClockingIT
http://<%= @user.company.subdomain%>.<%= $CONFIG[:domain] %>/tasks/view/<%=@task.task_num%>
