<%= form_for(@org_unit, :html => {:class => "form-horizontal"}) do |f| %>
  <fieldset>
    <%= f.error_messages %>
    <%= f.hidden_field(:customer_id) %>

    <div class="control-group">
      <label for="organization_unit_name"><%= t("org_units.name") %></label>
      <div class="controls">
        <%= f.text_field :name %>
      </div>
    </div>

    <div class="control-group">
      <label for="organization_unit_name"><%= t("org_units.active") %></label>
      <div class="controls">
        <%= f.check_box :active %>
      </div>
    </div>

    <%= render(:partial => "/common/custom_attributes", :locals => { :object => @org_unit }) %>

    <div class="form-actions">
      <%= cit_submit_tag(@org_unit) %>
      <% if !@org_unit.new_record? %>
        <%= link_to(t("button.delete"),
	  organizational_unit_path(@org_unit, :customer_id => @org_unit.customer.id),
          :method => :delete, :confirm => t("shared.are_you_sure"), :class => "btn btn-danger") %>
      <% end %>
    </div>
  </fieldset>
<% end %>
