<tr>
  <td> <%= score_rule.name %></td>
  <td> <%= score_rule.score %></td>
  <td> <%= score_rule.exponent %></td>
  <td> <%= score_rule.score_type %></td>
  <td> 
    <%= link_to 'edit', 
                edit_score_rule_path_for(container, score_rule),
                :class => 'edit-score-rule' %></td>
  <td>
    <%= link_to 'delete', 
                score_rule_path_for(container, score_rule),
                :class    => 'delete-score-rule' %>
  </td>
</tr>
