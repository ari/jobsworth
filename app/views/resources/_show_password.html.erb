<%
   name = "resource[attribute_values][][value]"
   id = "attribute_#{ @attribute.id }"
%>
<%=
   field = text_field_tag(name, @attribute.value, :id => id, :class => "value",
                           :size => @attribute.resource_type_attribute.default_field_length)
%>
