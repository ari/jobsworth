<%= @tasks.collect{|task|
  date = tz.utc_to_local(task.estimate_date).strftime('%Y-%m-%d')
  {:id       => task.task_num,
   :title    => task.name,
   :start    => date,
   :end      => date,
   :resource => task.task_num
  }
}.to_json.html_safe
-%>
