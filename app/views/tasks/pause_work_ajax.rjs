page << "if($('task_#{@sheet.task_id}')) {"
page.replace "task_#{@sheet.task_id}", :partial => "task_row", :locals => {:task => @sheet.task, :depth => params[:depth].to_i || 0 }
page << "}"
page.replace_html "menu_info", :partial => "layouts/sheet_info"
page.call "UpdateDnD"

