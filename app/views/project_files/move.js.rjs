page.replace_html "folder_info_#{@folder.id}", :partial => "folder_info", :locals => { :folder => @folder, :up => @up }
#page.visual_effect(:highlight, "folder_cell_#{@folder.id}", :duration => 2.0)

