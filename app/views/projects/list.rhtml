<table class="content project-list" width="100%" style="padding-left: 1em; padding-right: 1em;" cellpadding="0" cellspacing="0">
  <tr>
    <td class="page_header">
      <div style="float:left;"><%= current_user.company.name %> <%=_ 'Projects' %></div>
      <div style="float:right;padding-top:0.1em;"><%= link_to _('New Project'), :action => 'new' %></div>
    </td>
  </tr>
  <tr>
    <td>
      <% client = nil
      for @project in @projects 
        if client != @project.customer_id
      %>
      <%= "</div>" unless client.nil? %>
      <% client = @project.customer_id %>
      <div id="<%=@project.customer.dom_id%>" class="client-block">
	<div class="client-heading hoverrow">
	  <div class="client-name"><%= "<a href=\"/views/select_client/#{@project.customer_id}\">#{h(@project.customer.name)}</a>" %></div>
	</div>
      <% end %>

      <%= render :partial => "project" %>
      <% end %>
      </div>
    </td>
  </tr>
</table>
<br />
