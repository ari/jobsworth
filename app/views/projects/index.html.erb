<% @page_title = t("projects.index_title", title: Setting.productName) %>


<legend><%= t("projects.projects") %></legend>

<%= render :partial => "project", :collection => @projects %>

<% if current_user.completed_projects.size > 0 %>
  <%= link_to t("projects.n_completed_projects", n: current_user.completed_projects.size), :controller => 'projects', :action => 'list_completed' %>
<% end %>

