<div class="well" style="padding: 8px 0;">
  <ul class="nav nav-list">
    <li class="nav-header">
      <%= user.name %>
    </li>

    <li class="<%= active_class(selected, "general") %>">
      <a href="<%= edit_user_path(user) %>">General</a>
    </li>

    <% if current_user.admin? %>
    <li class="<%= active_class(selected, "access") %>">
      <a href="<%= access_user_path(user) %>">Access Control</a>
    </li>
    <% end %>

    <li class="<%= active_class(selected, "emails") %>">
      <a href="<%= emails_user_path(user) %>">Email Addresses</a>
    </li>

    <li class="<%= active_class(selected, "filters") %>">
      <a href="<%= filters_user_path(user) %>">Task Filters</a>
    </li>

    <% if current_user.admin? %>
    <li class="<%= active_class(selected, "projects") %>">
      <a href="<%= projects_user_path(user) %>">Projects Permissions</a>
    </li>
    <% end %>

    <li class="<%= active_class(selected, "tasks") %>">
      <a href="<%= tasks_user_path(user) %>">Tasks</a>
    </li>

    <li class="<%= active_class(selected, "workplan") %>">
      <a href="<%= workplan_user_path(user) %>">Work Plan</a>
    </li>
  </ul>
</div>

