<%= fields.error_messages %>
<legend> Reassign task to </legend>
<p>
  <%= fields.label :user_id, _("User") %>
  <%= fields.select :user_id, objects_to_names_and_ids(current_user.company.users.limit(20)) %>
</p>


